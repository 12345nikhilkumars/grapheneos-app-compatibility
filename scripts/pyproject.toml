[project]
name = "grapheneos-info-board-tools"
version = "0.1.0"
description = "Validation and data derivation for the GrapheneOS Info Board"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.23",
    "pyyaml>=6.0",
]
